---
title: Timeline
description: A flexible scroll-driven timeline with animated progress, configurable orientations, and customizable pins.
author:
  name: raouf.codes
  url: https://github.com/Gattalraouf
releaseDate: 2026-03-01
---

## Overview

The `Timeline` component provides a scroll-driven timeline experience with support for both horizontal and vertical orientations. It features animated progress bars, customizable pins, and smooth transitions.

By default, `Timeline` automatically switches to vertical layout on mobile screens.

<ComponentPreview name="demo-components-timeline" />

## Installation

<ComponentInstallation name="components-timeline" />

## Usage

### Basic

```tsx
import { Timeline } from '@/components/azemmur/components/timeline';

export default function Example() {
  return (
    <Timeline>
      <Timeline.Item title="2020">
        <div>First milestone</div>
      </Timeline.Item>
      <Timeline.Item title="2021">
        <div>Second milestone</div>
      </Timeline.Item>
      <Timeline.Item title="2022">
        <div>Third milestone</div>
      </Timeline.Item>
    </Timeline>
  );
}
```

### Vertical Orientation

```tsx
<Timeline orientation="vertical">
  <Timeline.Item title="Step 1">
    <p>First step content</p>
  </Timeline.Item>
  <Timeline.Item title="Step 2">
    <p>Second step content</p>
  </Timeline.Item>
</Timeline>
```

### Separate Components

Use dedicated components when you want an explicit layout regardless of viewport:

```tsx
<Timeline.Horizontal>
  <Timeline.Item title="Step 1">
    <p>Horizontal content</p>
  </Timeline.Item>
</Timeline.Horizontal>

<Timeline.Vertical>
  <Timeline.Item title="Step 1">
    <p>Vertical content</p>
  </Timeline.Item>
</Timeline.Vertical>
```

### With Header

```tsx
<Timeline intent="primary">
  <Timeline.Header>
    <h2 className="text-4xl font-bold">My Journey</h2>
  </Timeline.Header>

  <Timeline.Item title="2020">
    <div>First milestone</div>
  </Timeline.Item>
</Timeline>
```

### Variant Examples

#### Size Variants

```tsx
<Timeline size="sm">...</Timeline>
<Timeline size="md">...</Timeline>
<Timeline size="lg">...</Timeline>
```

#### Gradient Variants

```tsx
<Timeline gradient="purple-blue">...</Timeline>
<Timeline gradient="orange-red">...</Timeline>
<Timeline gradient="green-teal">...</Timeline>
<Timeline gradient="rainbow">...</Timeline>
<Timeline gradient="none">...</Timeline>
```

#### Visual Styles

```tsx
<Timeline visuals="card">...</Timeline>
<Timeline visuals="bordered">...</Timeline>
<Timeline visuals="solid">...</Timeline>
<Timeline visuals="dashed">...</Timeline>
<Timeline visuals="dotted">...</Timeline>
```

#### Pin Shapes

```tsx
<Timeline shape="circle">...</Timeline>
<Timeline shape="ring">...</Timeline>
<Timeline shape="dot">...</Timeline>
<Timeline shape="square">...</Timeline>
<Timeline shape="diamond">...</Timeline>
```

### Mobile Behavior

`Timeline` switches from horizontal to vertical on mobile by default. You can disable this behavior with `responsive={false}`.

```tsx
import { Timeline } from '@/components/azemmur/components/timeline';

export function TimelineExample() {
  return (
    <Timeline orientation="horizontal" responsive={false}>
      <Timeline.Item title="2020">
        <p>Content</p>
      </Timeline.Item>
    </Timeline>
  );
}
```

### LTR Support

Use the `dir` prop to control reading direction:

```tsx
<Timeline dir="ltr">
  <Timeline.Item title="2020">
    <div>Content</div>
  </Timeline.Item>
</Timeline>
```

## API Reference

### `Timeline`

<TypeTable
  type={{
    children: {
      description: 'Timeline.Header and Timeline.Item components.',
      type: 'React.ReactNode',
      required: true,
    },
    orientation: {
      description:
        'Preferred timeline layout orientation. Horizontal automatically switches to vertical on mobile unless responsive is false.',
      type: 'enum',
      typeDescription: '"horizontal" | "vertical"',
      required: false,
      default: '"horizontal"',
    },
    responsive: {
      description:
        'When true, horizontal orientation becomes vertical on mobile screens.',
      type: 'boolean',
      required: false,
      default: 'true',
    },
    mobileBreakpoint: {
      description:
        'Viewport width threshold used to detect mobile for responsive orientation switching.',
      type: 'number',
      required: false,
      default: '1024',
    },
    intent: {
      description: 'Color intent for pins and progress bar.',
      type: 'enum',
      typeDescription:
        '"primary" | "accent" | "secondary" | "success" | "info" | "warning" | "error"',
      required: false,
      default: '"primary"',
    },
    size: {
      description:
        'Controls pin size, title size, spacing, and progress bar thickness.',
      type: 'enum',
      typeDescription: '"sm" | "md" | "lg"',
      required: false,
      default: '"md"',
    },
    gradient: {
      description: 'Progress bar gradient style.',
      type: 'enum',
      typeDescription:
        '"purple-blue" | "orange-red" | "green-teal" | "rainbow" | "none"',
      required: false,
      default: '"purple-blue"',
    },
    visuals: {
      description: 'Content item visual styling.',
      type: 'enum',
      typeDescription:
        '"card" | "bordered" | "solid" | "dashed" | "dotted" | "none"',
      required: false,
      default: '"none"',
    },
    shape: {
      description: 'Pin shape style.',
      type: 'enum',
      typeDescription: '"circle" | "ring" | "dot" | "square" | "diamond"',
      required: false,
      default: '"circle"',
    },
    dir: {
      description: 'Controls reading direction.',
      type: 'enum',
      typeDescription: '"ltr" | "rtl"',
      required: false,
      default: '"ltr"',
    },
    className: {
      description: 'Additional CSS classes for the timeline container.',
      type: 'string',
      required: false,
    },
    progressClassName: {
      description: 'Additional CSS classes for the progress bar.',
      type: 'string',
      required: false,
    },
  }}
/>

### `Timeline.Header`

<TypeTable
  type={{
    children: {
      description: 'Content to display as the timeline header.',
      type: 'React.ReactNode',
      required: true,
    },
    orientation: {
      description: 'Inherits from parent Timeline if not specified.',
      type: 'enum',
      typeDescription: '"horizontal" | "vertical"',
      required: false,
    },
    className: {
      description: 'Additional CSS classes for the header container.',
      type: 'string',
      required: false,
    },
  }}
/>

### `Timeline.Item`

<TypeTable
  type={{
    title: {
      description: 'Title displayed with the pin.',
      type: 'string',
      required: true,
    },
    children: {
      description: 'Content to display in the timeline item.',
      type: 'React.ReactNode',
      required: true,
    },
    intent: {
      description: 'Override parent intent for this item.',
      type: 'enum',
      typeDescription:
        '"primary" | "accent" | "secondary" | "success" | "info" | "warning" | "error"',
      required: false,
    },
    size: {
      description: 'Override parent size for this item.',
      type: 'enum',
      typeDescription: '"sm" | "md" | "lg"',
      required: false,
    },
    visuals: {
      description: 'Override parent visuals for this item.',
      type: 'enum',
      typeDescription:
        '"card" | "bordered" | "solid" | "dashed" | "dotted" | "none"',
      required: false,
    },
    shape: {
      description: 'Override parent shape for this item.',
      type: 'enum',
      typeDescription: '"circle" | "ring" | "dot" | "square" | "diamond"',
      required: false,
    },
    className: {
      description: 'Additional CSS classes for the item container.',
      type: 'string',
      required: false,
    },
    contentClassName: {
      description: 'Additional CSS classes for the content wrapper.',
      type: 'string',
      required: false,
    },
    pinClassName: {
      description: 'Additional CSS classes for the pin.',
      type: 'string',
      required: false,
    },
  }}
/>

## Accessibility

The Timeline component includes the following accessibility features:

- Semantic HTML with `<section>`, `role="list"`, and `role="listitem"`
- Proper heading hierarchy with `aria-labelledby` relationships
- Progress indicators are decorative and hidden from screen readers
- Scroll-driven animations respect `prefers-reduced-motion`

## Credits

- Inspired by horizontal scroll timelines and scroll-driven animation patterns.
