---
title: Blur Image
description: Image component with animated shimmer placeholder and blur-to-clear loading effect.
author:
  name: raouf.codes
  url: https://github.com/Gattalraouf
releaseDate: 2026-02-01
---

<ComponentPreview name="demo-components-blur-image" />

## Overview

### Core Concept

The `BlurImage` component wraps Next.js `Image` to provide an enhanced loading experience with a shimmer animation and blur-to-clear transition. It eliminates content layout shift while delivering a polished progressive loading effect.

### Key Features

- **Shimmer animation** — A subtle overlay sweeps across the placeholder while loading
- **Blur-to-clear transition** — Image starts blurred and scales down smoothly as it loads
- **Default placeholder** — Falls back to a neutral gray SVG placeholder
- **Custom placeholder support** — Provide your own `blurDataURL` for branded loading states
- **SSR compatible** — Works seamlessly with Next.js server-side rendering

## Installation

<ComponentInstallation name="components-blur-image" />

## Usage

### Basic

```tsx
<BlurImage src="/og-image.png" width={800} height={450} alt="Description" />
```

### Composition

Combine with grid layouts or card components for gallery views:

```tsx
<div className="grid grid-cols-2 gap-4">
  <BlurImage
    src="/photo-1.jpg"
    width={400}
    height={300}
    alt="Gallery image 1"
  />
  <BlurImage
    src="/photo-2.jpg"
    width={400}
    height={300}
    alt="Gallery image 2"
  />
</div>
```

### Custom Placeholder

You can provide your own base64-encoded placeholder image:

```tsx
<BlurImage
  src="/photo.jpg"
  width={800}
  height={450}
  alt="Photo"
  blurDataURL="data:image/png;base64,YOUR_BASE64_PLACEHOLDER..."
/>
```

## API Reference

### BlurImage

<TypeTable
  type={{
    src: {
      description: 'Image source (string or StaticImport).',
      type: 'string',
      required: true,
    },
    alt: {
      description: 'Alt text for the image (required for accessibility).',
      type: 'string',
      required: true,
    },
    width: {
      description: 'Width of the image in pixels.',
      type: 'number',
      required: false,
      default: '400',
    },
    height: {
      description: 'Height of the image in pixels.',
      type: 'number',
      required: false,
      default: '210',
    },
    blurDataURL: {
      description:
        'Custom base64-encoded placeholder image. Falls back to a neutral gray placeholder if not provided.',
      type: 'string',
      required: false,
    },
    className: {
      description: 'Additional class names applied to the container wrapper.',
      type: 'string',
      required: false,
    },
    '...props': {
      description: 'Other Next/Image props (loading, priority, quality, etc.).',
      type: 'ImageProps',
      required: false,
    },
  }}
/>

## Accessibility

- **Required `alt` attribute** — Enforces descriptive alt text for screen readers
- **Decorative images** — Use `alt=""` for purely decorative images
- **Reduced motion** — Shimmer animation respects `prefers-reduced-motion` when styled appropriately

## Notes

- The shimmer animation requires the `shimmer` keyframe in your CSS. Add this to your global styles:

```css
@keyframes shimmer {
  0% {
    transform: translateX(-100%);
  }
  100% {
    transform: translateX(100%);
  }
}
```

- For production, consider a server-side preprocessing step to generate low-resolution data URLs for all remote assets.

## Credits

- Inspired by Next.js Image placeholder patterns
- Shimmer effect inspired by [Skeleton loading patterns](https://css-tricks.com/building-skeleton-screens-css-custom-properties/)
